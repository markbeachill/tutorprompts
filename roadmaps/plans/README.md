# Plans

This folder contains planning specifications for possible future toolkit changes. Files here are not live prompt-library behaviour until they are explicitly implemented in `src/`, rebuilt and tested.

Current plans:

- `in-tool-help-system-spec.md` — specification for a possible future in-tool help system and optional `EAL on` mode.
