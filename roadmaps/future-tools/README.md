# Future tools

Future or proposed tools live here until they are accepted into the active prompt-library source.

A document in this folder is not active toolkit behaviour unless the user explicitly asks for it to be integrated and the source files under `src/` are updated.
