# AI Personal Tutor Toolkit — site update v2.12

## Purpose

This is a formatting-only rebuild made after spotting a header/layout problem on `testing.html`.

## Changes

- Rebuilt `testing.html` to match the shared page template used by the rest of the site.
- Restored the standard branded site header and navigation.
- Moved the Testing and audit title into the normal hero section inside `<main>`.
- Changed the testing page button wrapper from `button-row` to the shared `btn-row` pattern.
- Added a CSS alias so both `btn-row` and `button-row` render consistently if either appears in future pages.
- Added a changelog entry for v2.12.

## Content changes

No prompt-library or audit-pack content changed. Current content versions remain:

- Prompt libraries: v2.4
- Testing/audit pack: v2.8
- Site package: v2.12
